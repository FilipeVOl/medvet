// import { render, fireEvent } from "@testing-library/react";
// import TelaConsulta from "../Component/TelaConsulta";

// test("renders TelaConsulta and checks user interactions", () => {
//   const { getByLabelText } = render(<TelaConsulta />);

//   const pacienteDropdown = getByLabelText("Paciente");
//   const tutorDropdown = getByLabelText("Tutor");

//   fireEvent.change(pacienteDropdown, { target: { value: "Test Paciente" } });
//   expect(pacienteDropdown.value).toBe("Test Paciente");

//   fireEvent.change(tutorDropdown, { target: { value: "Test Tutor" } });
//   expect(tutorDropdown.value).toBe("Test Tutor");
// });
